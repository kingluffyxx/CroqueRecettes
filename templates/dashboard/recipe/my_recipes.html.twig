{% extends 'base.html.twig' %}

{% block title %}DashboardController!
{% endblock %}

{% block body %}
	{# <div>
					<h1>Mes recettes</h1>
				</div> #}


	<div>

		<div {{ react_component('RecipesSection', {'recipes': recipes, 'idModal': 'editRecipe', 'userFavoriteIds': favorites, 'type' : type} ) }}></div>
		<div {{ react_component('SpeedDial', {'idModal': 'createRecipe'} ) }}></div>
	</div>

	<dialog id="createRecipe" class="modal">
		<div class="w-11/12 max-w-2xl modal-box">
			<form method="dialog">
				<button class="top-2 right-2 absolute btn btn-sm btn-circle btn-ghost">✕</button>
			</form>
			<twig:CreateRecipe/>
		</div>
		<form method="dialog" class="modal-backdrop">
			<button>close</button>
		</form>
	</dialog>
{% endblock %}
